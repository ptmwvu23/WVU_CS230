import { Component } from "@angular/core";

@Component({
    selector: "HockeyReference-players",
    templateUrl: "players.component.html"
})
export class PlayersComponent{
    Header: string = "Every NHL Player"
}