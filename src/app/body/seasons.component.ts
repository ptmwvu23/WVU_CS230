import { Component } from "@angular/core";

@Component({
    selector: "HockeyReference-seasons",
    templateUrl: "seasons.component.html"
})
export class SeasonsComponent{
    Header: string = "Every NHL Player"
}